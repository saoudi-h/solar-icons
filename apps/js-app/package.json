{
  "name": "js-app",
  "private": true,
  "version": "2.0.0-beta.0",
  "type": "module",
  "scripts": {
    "predev": "pnpm prepare:catalog",
    "dev": "vite",
    "prebuild": "pnpm prepare:catalog",
    "build": "tsc -b && vite build",
    "prepare:catalog": "pnpm -w prepare:visual-package @solar-icons/js",
    "preview": "vite preview"
  },
  "dependencies": {
    "@solar-icons/js": "workspace:*"
  },
  "devDependencies": {
    "typescript": "~5.6.2",
    "vite": "^8.2.0"
  }
}
