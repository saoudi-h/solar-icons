---
title: Solar Icons
description: 1,246 icons in 6 styles. One package per framework. CSS variables, duotone, and tree-shakable imports.
icon: HandShakeIcon
---

import { Cards, Card } from 'fumadocs-ui/components/card'

<Callout type="warn">
    Solar Icons v3 is in beta. [Report issues on
    GitHub](https://github.com/saoudi-h/solar-icons/issues).
</Callout>

Solar Icons gives you 1,246 distinct icons in six styles: **Bold**, **Linear**, **Outline**, **BoldDuotone**, **LineDuotone**, and **Broken**. With 7,476 total variations.

## Frameworks

<Cards>
    <Card
        href="/docs/v3/frameworks/react"
        title="React"
        description="React 18+ with SolarProvider, useSolar, and CSS variables."
    />
    <Card
        href="/docs/v3/frameworks/vue"
        title="Vue"
        description="Vue 3.0+ with SolarProvider, useSolar, and CSS variables."
    />
    <Card
        href="/docs/v3/frameworks/svelte"
        title="Svelte"
        description="Svelte 5 runes with $bindable provider and named exports."
    />
    <Card
        href="/docs/v3/frameworks/solid"
        title="SolidJS"
        description="SolidJS 1.6+ with signal-based useSolar."
    />
    <Card
        href="/docs/v3/frameworks/angular"
        title="Angular"
        description="Angular 17+ standalone components with signal-based inputs."
    />
    <Card
        href="/docs/v3/frameworks/react-native"
        title="React Native"
        description="React Native 0.72+ with react-native-svg."
    />
    <Card
        href="/docs/v3/frameworks/nuxt"
        title="Nuxt"
        description="Nuxt 3+ module with auto-import and configurable prefix."
    />
</Cards>

## What changed in v3

The v3 release unifies the library under a single API across every framework.

- **One package per framework.** `@solar-icons/react`, `@solar-icons/vue`, `@solar-icons/svelte`, `@solar-icons/solid`, `@solar-icons/angular`, `@solar-icons/react-native`, and `@solar-icons/nuxt`. The `@solar-icons/react-perf` package is consolidated into `@solar-icons/react`.
- **Classic mode by default.** Every icon ships with a single style per component, tree-shakable at the import level. Dynamic style switching uses a separate `dynamic` import path.
- **CSS variable cascade.** `SolarProvider` sets `--solar-color`, `--solar-size`, `--solar-stroke-width`, and duotone variables on a wrapper element. Icons inherit these through normal CSS inheritance, no re-renders on change.
- **Duotone built in.** `secondaryColor` and `secondaryOpacity` props control the duotone accent path.
- **Stroke width control.** Linear, Broken, and LineDuotone icons accept `strokeWidth`. Default is `1.5`.
- **`Icon` suffix on component names.** `HomeBoldIcon`, not `Home`. Angular uses a `Solar` prefix (`SolarHomeBold`) instead.
- **Kebab-case import paths.** `@solar-icons/react/bold/heart`, not `@solar-icons/react/Bold/Heart`.

## Icons page

Browse and search all 1,246 icons with live style previews, color controls, and duotone configuration.

[Explore Icons →](/icons)

## Migration

If you are upgrading from an older version, follow the [migration guide](/docs/v3/migration-to-v3) for a step-by-step walkthrough of every breaking change.

## Acknowledgements

This project wraps the **Solar Icon Set** created by **480 Design**, licensed under [CC BY 4.0](https://www.figma.com/community/file/1166831539721848736).

Design inspiration comes from **Phosphor Icons** and **Lucide Icons**.
