---
title: Static
description: Static assets and utilities for Solar Icons that work without JavaScript frameworks. This package provides individual SVG files, an SVG sprite, and Node.js utilities for server-side string templating.
icon: file-type-html
---

```package-install
@solar-icons/static
```

**What you can accomplish:**

- Embed raw SVGs or an SVG sprite directly in HTML
- Import SVG strings in Node.js applications and server-side rendering
- Build static websites and applications without JavaScript framework dependencies

This package includes the following implementations of Solar Icons:

- Individual SVG files
- SVG sprite
- A JavaScript library exporting SVG strings
- JSON Metadata

## SVG Sprite

The package includes a pre-built sprite containing all the icons.

#### Basic sprite usage:

```html
<img src="@solar-icons/static/sprite.svg#heart-bold" />
```

#### Inline with CSS helper class

If you'd prefer, you can use CSS to hold your base SVG properties. Unlike standard SVG properties, Solar Icons rely on specific CSS variables to control their rendering, especially for duotone styles.

`icon.css`:

```css
.solar-icon {
    /* Default variables */
    --solar-color: currentColor;
    --solar-size: 24px;

    /* For Linear, Broken and LineDuotone */
    --solar-stroke-width: 1.5;

    /* For duotone styles */
    --solar-secondary-color: currentColor;
    --solar-secondary-opacity: 0.5;

    width: var(--solar-size);
    height: var(--solar-size);
}
```

`index.html`:

```html
<!DOCTYPE html>
<html>
    <body>
        <!-- Use the CSS helper class along with the icon-specific class -->
        <svg xmlns="http://www.w3.org/2000/svg" class="solar-icon solar-heart-bold-duotone">
            <use href="#heart-bold-duotone" />
        </svg>

        <!-- Inject the sprite here -->
        <div id="sprite" style="display: none;"></div>

        <script type="module" src="index.js"></script>
    </body>
</html>
```

`index.js`:

```js
import './icon.css'
// With bundlers like Vite, import the raw SVG string
import sprite from '@solar-icons/static/sprite?raw'

document.getElementById('sprite').innerHTML = sprite
```

## SVG file as string

To import an SVG as a string (e.g., for templating):

::: code-group

```js [Webpack]
// Specific icon style
import heartIcon from '@solar-icons/static/dist/icons/bold/heart.svg'
```

```js [Vite]
// Specific icon style
import heartIcon from '@solar-icons/static/dist/icons/bold/heart.svg?raw'
```

:::

## Node.js

You can import Solar Icons as SVG strings directly in Node.js projects. `@solar-icons/static` is an ESM-only package.

```js [ESM]
import { HeartBoldIcon } from '@solar-icons/static'

// You can also import specific styles for lighter resolution
import { HeartIcon } from '@solar-icons/static/bold'
```

> Note: Each icon export name is in PascalCase and includes the style suffix (e.g. `BoldIcon`, `LinearIcon`) when imported from the top level.

#### Example with Node.js

```js
import http from 'http'
import { HeartBoldDuotoneIcon } from '@solar-icons/static'

const server = http.createServer((req, res) => {
    res.statusCode = 200
    res.setHeader('Content-Type', 'text/html')

    res.end(`
    <!DOCTYPE html>
    <html>
      <head>
        <style>
          .solar-icon {
            --solar-color: #ef4444;
            --solar-size: 64px;
            --solar-secondary-color: #fca5a5;
            --solar-secondary-opacity: 0.8;
          }
        </style>
      </head>
      <body>
        <h1>Solar Icons</h1>
        <p>This is a Solar icon:</p>
        <div class="solar-icon">
          ${HeartBoldDuotoneIcon}
        </div>
      </body>
    </html>
  `)
})

const hostname = '127.0.0.1'
const port = 3000

server.listen(port, hostname, () => {
    console.log(`Server running at http://${hostname}:${port}/`)
})
```

## Metadata & Collections

If you are building an icon picker or need a searchable list, you can import the metadata JSON files.

```ts
// Complete metadata with tags and categories
import metadata from '@solar-icons/static/metadata.json' with { type: 'json' }

// Just the list of icon names and styles
import icons from '@solar-icons/static/icons.json' with { type: 'json' }
```
