/**
 * Shared layout measurements for the site header.
 *
 * Keep the visual height and the top offset separate so consumers can
 * reserve the complete header footprint without duplicating measurements.
 */
export const SITE_HEADER_HEIGHT = 56
export const SITE_HEADER_OFFSET = 16
export const SITE_HEADER_RESERVED_HEIGHT = SITE_HEADER_HEIGHT + SITE_HEADER_OFFSET
