# AGENT: apps/react-app

- Next.js demo for `@solar-icons/react` (recommended package). Validates CSS vars, classes, SolarProvider, useSolar, duotone, accessibility.
- Ignored from changesets. Not deployed.
- The Lucide parity/mapping workbench lives in `apps/icon-parity`; keep this app focused on visual package testing.
- `dev` and `build` refresh the generated catalog and rebuild `@solar-icons/react` automatically.
