# AGENT: apps/vue-app

- Vite-based Vue 3 demo for `@solar-icons/vue`. Validates the package locally.
- Ignored from changesets. Not deployed.
- `dev` and `build-only` refresh the generated catalog and rebuild `@solar-icons/vue` automatically.
